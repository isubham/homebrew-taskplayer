# macOS
.DS_Store

# Node
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Rust / Cargo (build artifacts) — Cargo.lock is committed for apps
target/
src-tauri/target/
**/*.rs.bk

# Tauri (generated)
src-tauri/gen/schemas/

# Local runtime data (SQLite + the auth session file live in the OS
# app-data dir, but ignore any local copies)
*.sqlite3
*.sqlite
*.db
session.json

# Editors / env
.vscode/
.idea/
.env
.env.*
*.swp

# Local Claude Code settings (machine-specific bash permissions, not project config)
.claude/

concepts/

dist_verify/.env

output/

.pnpm/
.pnpm-store/

